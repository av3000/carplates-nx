import { backendExpressCarplateControllers } from './backend-express-carplate-controllers';

describe('backendExpressCarplateControllers', () => {
  it('should work', () => {
    expect(backendExpressCarplateControllers()).toEqual(
      'backend-express-carplate-controllers'
    );
  });
});
